apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "democratic-csi.fullname" . }}-test-connection"
  labels:
    {{- include "democratic-csi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "democratic-csi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
