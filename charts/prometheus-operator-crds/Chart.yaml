apiVersion: v2
name: prometheus-operator-crds
description: Prometheus Operator CRDs (installed before kube-prometheus-stack to avoid annotation size limits)
type: application
version: 0.1.0
dependencies:
  - name: prometheus-operator-crds
    version: 28.0.1
    repository: https://prometheus-community.github.io/helm-charts
