external-secrets:
  grafanaDashboard:
    enabled: true

  metrics.service.enabled: true

  serviceMonitor:
    enabled: false
    namespace: prometheus
    interval: 30s
    scrapeTimeout: 10s

  extraArgs:
    store-requeue-interval: 30m

reloader:
  logFormat: json
  enableMetricsByNamespace: true

  deployment:
    replicas: 1

  serviceMonitor.enabled: true
  podMonitor.enabled: true
  